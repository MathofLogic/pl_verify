[project]
name = "pl-verify"
version = "0.1.0"
description = "The model proposes; PL disposes. A sealed, tiered verification harness for math derivations — a grader, never a solver."
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = ["sympy>=1.12"]
